# Copy to .env.local
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Comma-separated list of admin emails allowed to access /admin (MVP client-side gate)
NEXT_PUBLIC_ADMIN_EMAILS=admin@example.com

# App URLs
NEXT_PUBLIC_BASE_URL=http://localhost:3000

# Server-side (keep private in production)
SUPABASE_SERVICE_ROLE_KEY=

# Payments (Xendit)
XENDIT_API_KEY=
XENDIT_WEBHOOK_SECRET=

# SMS (SemaphorePH)
SEMAPHORE_API_KEY=
SEMAPHORE_SENDER=
